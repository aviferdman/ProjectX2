{
  "name": "projectx2-orchestrator",
  "version": "1.0.0",
  "description": "Orchestrator for ProjectX2 AI Corp — manages the autonomous agent company loop",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "reset": "node dist/index.js --reset",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@github/copilot-sdk": "^0.2.0",
    "chokidar": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "tsx": "^4.19.0",
    "vitest": "^3.0.0",
    "@types/node": "^22.0.0"
  }
}
